[project]
name = "python-lab"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pandas",
  "numpy",
  "matplotlib",
  "seaborn",
  "requests",
  "beautifulsoup4",
  "openpyxl",
  "tqdm",
  "python-dotenv"
]
